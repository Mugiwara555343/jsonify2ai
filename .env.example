# .env.example
# Copy to ".env" for local dev. Do NOT commit your real .env.

# --- Ports ---
PORT_API=8082
PORT_WORKER=8090
PORT_WEB=5173

# --- External services (local-first; override if you run your own) ---
OLLAMA_URL=http://host.docker.internal:11434
QDRANT_URL=http://host.docker.internal:6333
# Example DSN; adjust user/pass/db to your local setup
POSTGRES_DSN=postgresql://user:pass@host.docker.internal:5432/jsonify2ai

# --- Embeddings / worker defaults ---
EMBEDDINGS_MODEL=nomic-embed-text
EMBEDDING_DIM=768
# 1 = deterministic dummy embeddings (no Ollama); 0 = real embeddings
EMBED_DEV_MODE=0

# --- Text chunking ---
CHUNK_SIZE=800
CHUNK_OVERLAP=100

# --- Debug endpoints (/debug/config etc) ---
DEBUG_CONFIG=0

# --- Collections (text vs images) ---
# Text chunks collection used by /process/text
QDRANT_COLLECTION_CHUNKS=jsonify2ai_chunks
# Image embeddings collection used by /process/image
QDRANT_COLLECTION_IMAGES=jsonify2ai_images
